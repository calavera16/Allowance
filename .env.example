# Optional, local desktop configuration. The app needs no service API keys.
# Copy to .env for development; .env is ignored and excluded from release files.
# Set only a PUBLIC HTTPS update feed. Leave blank to disable update checks.
ALLOWANCE_UPDATE_URL=
# Never put credentials in VITE_ variables or in a public update URL.
# Keep signing certificates outside the project and passwords in a secret store.
